# 项目目标：四位数字验证码识别与智能评估系统

## 1. 项目定位

当前项目已经实现了基于 CNN 的单个手写数字识别功能，主要使用 MNIST 数据集训练模型，并通过 Gradio 画板完成单数字推理。

本次升级的目标是将项目扩展为一个完整的四位数字验证码识别系统：系统向用户展示一张经过适度模糊、扭曲和干扰处理的四位数字验证码图片，用户输入看到的数字，系统判断输入是否正确。

升级后的项目不只包含验证码识别界面，还应包含真实数字数据集管理、验证码数据构建、模型训练、模型评估、低质量样本过滤、错误样本解释和训练报告生成等功能，形成一个相对完整的机器学习工程项目。

## 2. 核心原则

本项目必须坚持以下原则：

- 模型训练必须基于真实数字图片数据。
- 不允许使用程序随机绘制数字作为主要训练数据。
- 不允许使用 AI 图片生成模型凭空生成数字图片冒充训练数据。
- 不允许使用固定字体批量渲染数字冒充真实图片。
- 可以进行数据增强，但增强必须作用于真实数字图片之上。
- 多模态大模型只能作为数据审核、错误解释和报告生成的辅助工具，不能替代真实标签，不能生成训练图片。

## 3. 总体目标

构建一个完整的四位数字验证码识别与智能评估系统，包括：

- 优化现有单数字识别模型，提高识别准确率和鲁棒性。
- 获取、整理并管理真实数字图片数据集。
- 基于真实数字图片构建四位数字验证码图片数据集。
- 对验证码图片进行适度模糊、扭曲、平移、缩放、噪声和干扰线处理。
- 训练模型识别验证码图片中的全部 4 个数字。
- 记录并维护每张验证码图片对应的真实标签。
- 实现验证码识别界面，判断用户输入是否正确。
- 实现验证码数据集管理模块，支持数据查看、筛选、统计和质量标记。
- 接入多模态大模型，辅助审核低质量样本。
- 接入大模型，对错误样本进行原因解释。
- 接入大模型，根据训练和评估结果自动生成训练报告。

## 4. 数据建设目标

### 4.1 数据来源要求

训练数据必须来自真实数字图片数据集，例如：

- 真实手写数字数据集。
- 真实印刷体数字数据集。
- 公开验证码或数字字符图片数据集。
- 从真实图片中裁剪、标注得到的数字样本。

所有数据来源应在文档中记录：

- 数据集名称。
- 下载来源或采集来源。
- 数据许可说明。
- 数据规模。
- 每类数字样本数量。
- 是否经过人工检查或自动清洗。

### 4.2 数据规模目标

目标数据应包含足够多且相对均衡的数字样本：

- 单数字图片覆盖 0 到 9 每个类别。
- 每个数字类别数量尽量均衡。
- 四位验证码图片覆盖不同数字组合。
- 训练集、验证集、测试集严格分离。
- 测试集不得参与训练、调参或数据增强参数选择。

### 4.3 四位验证码数据构建

基于真实单数字图片构建四位数字验证码图片：

- 每张验证码图片由 4 个真实数字样本组成。
- 每张验证码图片保存对应标签，例如 `5831`。
- 数字之间允许轻微间距变化、位置变化和大小变化。
- 每张图片需要记录来源数字样本、增强参数和最终标签。
- 处理强度不能过高，应保证人类用户可以正常判断。

### 4.4 验证码数据集管理模块

新增验证码数据集管理功能，用于查看、筛选和维护数据质量。

该模块应支持：

- 浏览原始单数字图片。
- 浏览构建后的四位验证码图片。
- 查看每张验证码图片的真实标签。
- 查看图片所属数据划分：训练集、验证集或测试集。
- 标记样本质量：正常、可疑、过度模糊、过度扭曲、标签疑似错误。
- 删除或排除低质量样本。
- 统计每个数字出现次数。
- 统计不同验证码长度、增强类型、模糊强度下的数据数量。
- 导出数据质量报告。

该模块可以先通过脚本和 CSV/JSON 文件实现，后续可扩展为 Gradio 或 Web 管理界面。

## 5. 图像处理目标

验证码图片需要进行适度干扰处理，用于模拟登录验证码场景。

可使用的处理方式包括：

- 轻度高斯模糊。
- 轻度运动模糊。
- 轻微旋转。
- 轻微透视变换。
- 轻微弹性扭曲。
- 水平或垂直方向少量平移。
- 背景噪声。
- 低强度干扰线。
- 轻微亮度和对比度变化。

处理限制：

- 不得让数字严重变形到人类无法识别。
- 不得遮挡关键笔画。
- 不得使用过强噪声破坏标签可靠性。
- 每张处理后的图片仍必须保留正确标签。
- 增强参数需要记录，方便复现实验和分析错误来源。

## 6. 模型建设目标

### 6.1 单数字识别模型优化

在现有 CNN 模型基础上进行优化：

- 修正当前训练流程中可能存在的数据泄漏问题。
- 使用规范的训练集、验证集、测试集划分。
- 增加针对真实数字图片的预处理流程。
- 提高模型对模糊、轻微扭曲和噪声的鲁棒性。
- 根据实际效果考虑升级为更强的 CNN、CRNN 或多输出模型。

### 6.2 四位验证码识别模型

最终模型需要输入一张四位验证码图片，并输出 4 个数字。

可选模型方案：

- 四输出头 CNN：共享特征提取层，每个输出头预测一个位置的数字。
- CNN + 序列模型：使用 CNN 提取图像特征，再使用 RNN、Transformer 或 CTC 解码数字序列。
- 先分割再识别：先定位四个数字区域，再分别调用单数字识别模型。

初版优先目标是稳定、可解释、易调试，因此可优先考虑“四输出头 CNN”或“先分割再识别”方案。

## 7. 大模型辅助能力目标

### 7.1 多模态大模型辅助数据质量审核

接入多模态大模型，用于抽样审核或批量辅助过滤低质量验证码图片。

输入内容：

- 验证码图片。
- 当前真实标签。
- 图片增强参数。

输出内容：

- 图片是否可由人类正常识别。
- 图片可能对应的数字。
- 是否存在过度模糊。
- 是否存在过度扭曲。
- 是否存在严重遮挡。
- 是否疑似标签错误。
- 建议保留、复查或剔除。

示例输出结构：

```json
{
  "human_readable": true,
  "estimated_text": "5831",
  "blur_level": "medium",
  "distortion_level": "low",
  "label_maybe_wrong": false,
  "suggestion": "keep"
}
```

约束：

- 多模态大模型审核结果只能作为辅助参考。
- 样本剔除策略需要保留日志。
- 对关键样本或争议样本应支持人工复核。
- 大模型不得生成训练验证码图片。

### 7.2 大模型错误样本解释

当验证码识别模型预测错误时，将错误样本交给大模型辅助分析错误原因。

输入内容：

- 验证码图片。
- 真实标签。
- 模型预测结果。
- 每一位数字的置信度。
- 图片增强参数。

输出内容：

- 错误原因解释。
- 是否存在数字粘连。
- 是否存在相似数字混淆，例如 3/8、1/7、5/6。
- 是否存在模糊过强或干扰线遮挡。
- 是否怀疑标签错误。
- 对数据增强或模型训练的改进建议。

错误解释结果应保存到错误分析报告中，用于后续改进数据和模型。

### 7.3 大模型自动生成训练报告

训练结束后，接入大模型根据训练日志、评估指标和错误分析结果生成 Markdown 训练报告。

报告内容应包括：

- 本次训练目标。
- 数据集来源和规模。
- 数据划分方式。
- 图像增强策略。
- 模型结构。
- 训练参数。
- 验证集与测试集表现。
- 单字符准确率。
- 四位验证码整体准确率。
- 混淆矩阵摘要。
- 典型错误样本分析。
- 多模态质检发现的问题。
- 下一轮优化建议。

报告应保存到 `reports/` 目录，便于项目展示、答辩和复现实验。

## 8. 评估目标

模型评估不能只看单个数字准确率，还必须关注整张验证码是否完全识别正确。

需要记录以下指标：

- 单个位置数字准确率。
- 单字符整体准确率。
- 四位验证码整体准确率。
- 每类数字混淆矩阵。
- 每个位置的混淆矩阵。
- 推理耗时。
- 错误样本数量与错误类型统计。
- 不同模糊、扭曲、噪声强度下的识别表现。

需要输出以下评估文件：

- `metrics.json`
- `confusion_matrix.png`
- `wrong_predictions.csv`
- `error_analysis.md`
- `training_report.md`

## 9. 界面目标

### 9.1 用户验证码识别界面

实现一个验证码识别界面，模拟登录验证码场景。

界面应包含：

- 验证码图片展示区域。
- 用户输入框。
- 提交按钮。
- 刷新验证码按钮。
- 判断结果提示。

基本交互流程：

1. 系统随机选择或构建一张基于真实数字数据的四位验证码图片。
2. 用户观察图片并输入四位数字。
3. 系统比较用户输入和验证码真实标签。
4. 如果一致，提示验证通过。
5. 如果不一致，提示验证失败并允许重新尝试或刷新验证码。

### 9.2 可选管理与分析界面

后续可扩展管理界面：

- 数据集浏览页面。
- 低质量样本复核页面。
- 错误样本浏览页面。
- 模型预测演示页面。
- 训练报告查看页面。

## 10. 推荐项目结构

升级后的项目可逐步整理为以下结构：

```text
project/
├─ app.py
├─ README.md
├─ PROJECT_GOALS.md
├─ requirements.txt
├─ data/
│  ├─ raw_digits/
│  ├─ captcha_images/
│  ├─ train.csv
│  ├─ val.csv
│  └─ test.csv
├─ src/
│  ├─ data/
│  │  ├─ download_dataset.py
│  │  ├─ build_captcha_dataset.py
│  │  ├─ augment.py
│  │  ├─ dataset_manager.py
│  │  └─ quality_check.py
│  ├─ models/
│  │  ├─ single_digit_cnn.py
│  │  ├─ captcha_cnn.py
│  │  └─ crnn.py
│  ├─ training/
│  │  ├─ train_single_digit.py
│  │  ├─ train_captcha.py
│  │  └─ evaluate.py
│  ├─ llm/
│  │  ├─ vision_quality_review.py
│  │  ├─ error_explainer.py
│  │  └─ report_generator.py
│  └─ ui/
│     ├─ gradio_app.py
│     └─ login_demo.py
├─ models/
│  ├─ single_digit_model.h5
│  └─ captcha_model.h5
├─ reports/
│  ├─ metrics.json
│  ├─ wrong_predictions.csv
│  ├─ error_analysis.md
│  ├─ training_report.md
│  └─ confusion_matrix.png
└─ notebooks/
   └─ experiments.ipynb
```

## 11. 项目交付物

升级后的项目应包含：

- 数据来源说明文档。
- 数据获取与整理脚本。
- 验证码数据集管理模块。
- 四位验证码数据集构建脚本。
- 图像模糊、扭曲、干扰处理脚本。
- 单数字模型训练脚本或 notebook。
- 四位验证码模型训练脚本或 notebook。
- 模型评估脚本。
- 多模态大模型数据质量审核模块。
- 大模型错误样本解释模块。
- 大模型训练报告生成模块。
- 训练好的验证码识别模型。
- 验证码识别界面应用。
- 项目运行说明文档。
- 训练报告和错误分析报告。

## 12. 验收标准

项目完成后应满足以下标准：

- 使用真实数字图片作为训练基础。
- 四位验证码图片均有可靠标签。
- 数据集管理模块可以查看、筛选和统计样本。
- 低质量样本可以被标记、复查或排除。
- 多模态大模型可以辅助审核样本质量。
- 模型可以识别每张图片中的 4 个数字。
- 测试集四位验证码整体识别准确率达到可接受水平。
- 错误样本可以被记录、查看并由大模型辅助解释。
- 训练完成后可以自动生成 Markdown 训练报告。
- 用户界面可以展示验证码并判断用户输入是否正确。
- 数据处理、训练、评估流程可以复现。
- README 或相关文档清楚说明数据来源、训练方法、运行方式和大模型辅助边界。

## 13. 实施阶段建议

推荐按以下阶段推进：

1. 整理当前项目结构，补充依赖文件和运行说明。
2. 修正并优化现有单数字模型训练流程。
3. 获取并清洗真实数字图片数据集。
4. 实现验证码数据集管理模块。
5. 构建四位验证码图片数据集和标签文件。
6. 实现适度模糊、扭曲、干扰增强流程。
7. 接入多模态大模型，辅助审核低质量样本。
8. 训练四位验证码识别模型。
9. 建立评估流程和错误样本分析流程。
10. 接入大模型进行错误样本解释。
11. 接入大模型自动生成训练报告。
12. 实现验证码识别界面。
13. 整合文档，完成可复现项目交付。
